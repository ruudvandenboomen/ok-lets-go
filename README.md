# OK LET'S GO

## Local environment
On the local environment you don't need Nginx and Certbot. So you can run the API by running:
```
poetry install
poetry shell
```

```
python3 ./api/app.py
```

## Deployment

```
docker compose up
```